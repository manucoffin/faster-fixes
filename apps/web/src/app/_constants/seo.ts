import { getAppUrl } from "@/utils/url/get-app-url";

export const SITE_NAME = "FasterFixes";
export const SITE_META_DESCRIPTION =
  "Client feedback that AI coding agents can fix directly. FasterFixes captures every technical detail automatically and outputs structured markdown your agent acts on without asking for context.";
export const DEFAULT_OG_IMAGE_URL = getAppUrl() + "/images/og-image.jpg";
export const ITEMS_PER_SITEMAP = 50000; // Google's recommended limit
