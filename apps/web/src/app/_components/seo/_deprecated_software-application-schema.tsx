// Retired: this JSON-LD emitter reads the subscription plans and prices, which
// is domain knowledge, and the root buckets are domain-agnostic (ADR-0010). It
// now lives at `(public)/_components/seo/software-application-schema.tsx`, next
// to the only route group that renders it. Nothing imports this module; it is
// kept as an empty stub for the maintainer to delete.
