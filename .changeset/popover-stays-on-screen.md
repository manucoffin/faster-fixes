---
"@fasterfixes/react": patch
---

The comment and pin popovers now stay inside the viewport when the selected element is taller than the screen. When neither side of the element has room, the popover sits against the visible edge of the viewport, over the element, instead of rendering off screen.
