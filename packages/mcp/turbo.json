{
  "$schema": "https://turborepo.dev/schema.v2.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "env": [
        "FASTER_FIXES_TOKEN",
        "FASTER_FIXES_PROJECT",
        "FASTER_FIXES_URL"
      ]
    }
  }
}
