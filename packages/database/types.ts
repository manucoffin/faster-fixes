// Public types entry point of the database package: the generated Prisma
// client is an implementation detail of this package (its folder is even
// gitignored), so a consumer reads row types, input types and the `Prisma`
// namespace through here rather than by deep path. The client instance itself
// stays at the package root (`@workspace/db`).
export * from "./generated/prisma/client";
